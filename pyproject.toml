#This file is used to package the InvenTreeLabelAutoPrint plugin.
#
#- It was generated by the InvenTree Plugin Creator tool - version 1.15.0
#- Ref: https://github.com/inventree/plugin_creator

[build-system]
requires = ["setuptools", "twine", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "inventree-label-auto-print"
description = "Triggers a print of a stock item label when a PO item is received"
dynamic = ["version"]
authors = [
    { name = "gunstr",email = "41468206+gunstr@users.noreply.github.com" },
]
readme = "README.md"
license = "MIT"
keywords = ["inventree", "plugin"]
requires-python = ">=3.9"
dependencies = [
    # Enter your plugin library dependencies here
]
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
    "Framework :: InvenTree",
]
[project.urls]
Homepage = "https://my-project-url.com"

[project.entry-points."inventree_plugins"]
InvenTreeLabelAutoPrint = "inventree_label_auto_print.core:InvenTreeLabelAutoPrint"

[tool.setuptools.packages.find]
where = [""]

[tool.setuptools.dynamic]
version = {attr = "inventree_label_auto_print.PLUGIN_VERSION"} 

[tool.ruff]
exclude = [
    ".git",
    "__pycache__",
    "test.py",
    "tests",
    "venv",
    "env",
    ".venv",
    ".env",
]

src=["inventree_label_auto_print"]
